import { ServicesTranslated } from "@/components/ServicesTranslated";

export default function ServicesPage() {
  return (
    <main className="min-h-screen bg-white dark:bg-zinc-950">
      <ServicesTranslated />
    </main>
  );
}
